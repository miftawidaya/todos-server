# Environment Variables
# Copy this file to .env.local and fill in the values

# ============================================
# REQUIRED VARIABLES
# ============================================

# JWT Secret Key (REQUIRED)
# Use a strong, random string for production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-key-change-this-in-production

# ============================================
# OPTIONAL VARIABLES
# ============================================

# JWT Token Expiration (optional, defaults to 24h)
# Examples: "1h", "24h", "7d", "30d"
JWT_EXPIRES_IN=24h

# Server Port (optional, defaults to 8080)
PORT=8080

# API URL for Swagger Documentation (optional)
# Auto-detected if not set:
#   - Vercel deployment → uses VERCEL_URL automatically
#   - Local development → uses http://localhost:8080
# Only set this if you need to override auto-detection
# API_URL=https://your-api.example.com

# Private API Key (optional)
# Only needed if you're using private/admin endpoints
# PRIVATE_API_KEY=your-private-api-key

# Node Environment (optional)
# Automatically set by Vercel to "production"
# For local development, defaults to "development"
# NODE_ENV=development
